[project]
group = "rip.jade"
artifact = "AdventOfCode"
version = "1.0.0"

[build]
java-version = "21"
encoding = "UTF-8"

[dependencies.main]
# Dependencies go here when added via `forge add`

[dependencies.test]
# Test dependencies go here
